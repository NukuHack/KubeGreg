/*
    stuff
*/

ServerEvents.recipes(event => {

  const greg = event.recipes.gtceu;

	// reactor make radiactive stuff + water = non radioactive stuff + radioactive steam
	// turbine make radioactive steam = water

/*
  greg
      .nuclear_reactor("gfs:x")
      .itemInputs('x')
      .inputFluids(Fluid.of('x', 2880))
      .itemOutputs('x')
      .duration(x)
      .EUt(x);
  greg
      .nuclear_turbine("gfs:x")
      .itemInputs('x')
      .inputFluids(Fluid.of('x', 2880))
      .itemOutputs('x')
      .duration(x)
      .EUt(x);
*/


});